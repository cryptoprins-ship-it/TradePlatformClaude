ANTHROPIC_API_KEY=sk-ant-your-key-here

# Copy this file to .env (gitignored). Agent loads it on startup via
# python-dotenv. Keeps the key out of your shell environment so Claude
# Code OAuth and other tools that rely on ANTHROPIC_API_KEY absence are
# not affected. An explicit env var in the shell still wins over .env.
